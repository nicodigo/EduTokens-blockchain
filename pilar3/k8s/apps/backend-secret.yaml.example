# Backend Secret — template (NO completar con valores reales aquí)
# Copiar a backend-secret.yaml y completar los valores antes de deployar.
apiVersion: v1
kind: Secret
metadata:
  name: backend-secret
  namespace: apps
  labels:
    app: backend
type: Opaque
stringData:
  JWT_SECRET: "{{ JWT_SECRET }}"
  ACADEMIC_AUTHORITY_PRIVATE_KEY: "{{ ACADEMIC_AUTHORITY_PRIVATE_KEY }}"
  ACADEMIC_AUTHORITY_PUBLIC_KEY: "{{ ACADEMIC_AUTHORITY_PUBLIC_KEY }}"
