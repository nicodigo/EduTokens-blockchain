# ── Intencionalmente eliminado ───────────────────────────────────
# Las Network Policies fueron removidas porque:
# 1. Rompieron DNS (blockchain → CoreDNS)
# 2. Rompieron cert-manager (ingress-nginx → solver ACME)
# 3. Bloquearon NCT → Redis
# 4. Docker Compose funciona sin ellas — no son necesarias para el MVP
#
# Si en el futuro se necesita aislamiento, volver a implementar
# comenzando con una regla por vez y testeando.
# ───────────────────────────────────────────────────────────────────
