# Handoff — EduTokens Blockchain (v3, FINAL)

> **Fecha:** 2026-06-20 (sesión 3: CI/CD completo + fixes deployados + docs)  
> **Repositorio:** `nicodigo/EduTokens-blockchain`  
> **Deadline:** 2026-06-23  
> **Handoff previo:** `pilar3/.artifacts/handoff-2026-06-20-v2.md`

---

## Resumen de la sesión 3

En esta sesión se completaron cuatro grandes bloques de trabajo:

1. **Deploy de fixes a GKE** — rebuild + push NCT/Pool, rollout restart, purge RabbitMQ, EARN tx exitosa
2. **CI/CD — Workload Identity Federation (OIDC)** — Pool + Provider creados en `pilar3/tofu/iam.tf`
3. **CI/CD — GitHub Actions workflows** — `gitleaks.yml` + `ci.yml` creados y funcionando
4. **Documentación** — `pilar3/README.md`, `project_overview.md` actualizado, `README.md` actualizado

Todo commiteado y pusheado a `main`. Los workflows de GitHub Actions pasan exitosamente.

---

## Estado del proyecto

### ✅ Funcionando en GKE

| Componente | Namespace | Estado |
|---|---|---|
| Redis | `infra` | Running (AOF, PVC 10Gi) |
| RabbitMQ | `infra` | Running (AMQP :5672, AMQPS :5671, TLS) |
| NCT | `blockchain` | Running (5 fixes deployados, 0 errores) |
| Pool-A | `blockchain` | Running (5 fixes deployados, 0 errores) |
| Worker GPU | `g-compumundo` | Deployado por el usuario (cluster del profesor, RTX 4060) |
| cert-manager | `cert-manager` | Running (Let's Encrypt production) |
| nginx-ingress | `ingress-nginx` | Running |

### ✅ CI/CD funcionando

| Workflow | Archivo | Acción |
|---|---|---|
| Gitleaks | `.github/workflows/gitleaks.yml` | Escanea secretos en push + PR |
| CI — Build & Push | `.github/workflows/ci.yml` | Build + push automático de 4 imágenes |

**Auth:** Workload Identity Federation OIDC. Pool: `projects/381458252403/locations/global/workloadIdentityPools/github-actions-oidc/providers/github-actions-provider`. SA: `github-actions@edutokens-2026.iam.gserviceaccount.com`.

### ✅ Documentación actualizada

| Archivo | Contenido |
|---|---|
| `pilar3/README.md` | Guía de despliegue completa (331 líneas) |
| `project_overview.md` | Pilar 3 actualizado con arquitectura cloud real |
| `README.md` | Badges CI/CD, tabla de pilares, tech stack |
| `pilar3/.artifacts/cloud-migration-plan.md` | Actualizado a sesión 3 |

---

## 5 bugs corregidos y deployados

Todos verificados con la sesión actual: rebuild → push → rollout restart → purge queues → test transaction.

| Bug | Archivo |
|---|---|
| NCT `StreamLostError` (RabbitMQ idle timeout) | `pilar2/nct/nct.py` |
| Worker `MinerError: empty stdout` → requeue infinito | `pilar2/miner/miner.py`, `pilar2/worker/worker.py` |
| Pool `PRECONDITION_FAILED` loop infinito | `pilar2/pool/pool.py` |
| `send_test_tx.py earn` nonce=0 siempre | `pilar2/tools/send_test_tx.py` |
| `GET /account/{pubkey}` 500 (.decode redundante) | `pilar2/storage/chain_store.py` |

---

## Archivos nuevos en esta sesión

```
.github/workflows/gitleaks.yml       # Secret scanning
.github/workflows/ci.yml             # Build + push automático
pilar3/README.md                     # Guía de despliegue
pilar3/.artifacts/handoff-2026-06-20-v3.md  # Este archivo
```

## Archivos modificados

```
README.md                            # Contenido completo (era stub de 1 línea)
project_overview.md                  # Pilar 3 actualizado, tech stack expandido
pilar3/tofu/iam.tf                   # +50 líneas: Workload Identity Federation
pilar3/.artifacts/cloud-migration-plan.md  # Fases 5 y 7 completadas
```

---

## Pendiente (mínimo)

1. **Video de demostración** — grabar el flujo: transacción → minado → bloque confirmado
2. Opcional: verificar que el worker GPU mina correctamente el bloque con la EARN tx pendiente

---

## Comandos rápidos para retomar

```bash
# Conectarse al cluster
gcloud container clusters get-credentials edutokens-cluster --zone us-central1-a --project edutokens-2026

# Ver pods
kubectl get pods -n blockchain
kubectl get pods -n infra

# Port-forward al NCT
kubectl -n blockchain port-forward svc/nct 8080:8080 &

# Health + status
curl localhost:8080/health
curl localhost:8080/status

# Enviar transacción de prueba
cd /home/nicolas/code/uni/EduTokens/EduTokens-blockchain
python3 pilar2/tools/send_test_tx.py gen
python3 pilar2/tools/send_test_tx.py earn <authority_privkey> <student_pubkey> 1000 "test"

# Ver workflows de CI/CD
open https://github.com/nicodigo/EduTokens-blockchain/actions

# OpenTofu (ver cambios en infra)
cd pilar3/tofu && tofu plan
```

---

## Suggested skills

- `diagnose` — si aparecen errores en los logs de NCT, Pool o Worker post-minado
- `v4-best-practices` — para cualquier modificación multi-archivo
- `handoff` — si se necesita pasar a otro agente
